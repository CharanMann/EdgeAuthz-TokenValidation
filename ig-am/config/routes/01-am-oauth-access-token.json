{
  "baseURI": "${frConfigs.amUrl}",
  "capture": "all",
  "condition": "${matches(request.uri.path, '^/am') and contains(request.uri.path, '/access_token') }",
  "handler": "ForgeRockClientHandler"
}
