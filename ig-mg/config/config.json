{
  "handler": {
    "name": "_router",
    "type": "Router"
  },
  "properties": {
    "frConfigs": {
      "$location": "${fileToUrl(openig.configDirectory)}/frProps.json"
    }
  },
  "heap": [
    {
      "name": "capture",
      "type": "CaptureDecorator",
      "config": {
        "captureEntity": false,
        "captureContext": false
      }
    }
  ]
}
